import 'package:flutter/material.dart';

class Tela5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Tela 5')),
      body: Center(child: Text('Conteúdo da Tela 5')),
    );
  }
}
